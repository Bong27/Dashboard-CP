// App.tsx is no longer used — main.tsx renders RouterProvider directly.
// Kept as a placeholder in case it's needed for future wrapping (providers, context, etc.)
export default function App() {
  return null;
}
